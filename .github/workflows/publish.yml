# Usage:
# - Runs automated tests to ensure the Java project builds correctly.
# - If the build is successful, it packages the application into a Docker container.
# - The container is pushed to GHCR and deployed to the vps.

# Requirements:
# - Config file on the vps. (should be named `{Environment}.config.json`)
#   - Example: `production.config.json`
#   - Environment default: production
# - These secrets must be set up in the repository settings:
#   - SSH_PRIVATE_KEY: Private SSH key for accessing the vps
#   - SSH_USER: The SSH username for the vps
#   - SSH_HOST: The SSH host (IP or domain) of the vps
#   - WORK_DIR: The directory on the vps where the app is/will be deployed
#   - BOT_TOKEN_{Environment}_{BranchName}: Environment variable for the bot
#     - Example: BOT_TOKEN_PRODUCTION_master
#     - Environment default: PRODUCTION
#   - MONGO_URI: Environment variable for the bot
#   - HYPIXEL_KEY: Environment variable for the bot
#   - CONFIG_PATH: Path to the config file on the vps

name: Publish

on:
  workflow_dispatch:
    inputs:
      botEnvironmentType:
        description: 'Bot environment type (All uppercase):'
        required: true
        default: 'PRODUCTION'
        type: string
      port:
        description: 'Port'
        required: true
        default: '8080'
        type: string

  workflow_run:
    workflows:
      - Java CI
    types:
      - completed

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: socksm/nerd-bot-${{ github.ref_name }}:latest
  CONTAINER_NAME: nerd-bot-${{ github.ref_name }}
  REPO_NAME: ${{ github.event.repository.name }}
  REPO_USERNAME: ${{ github.event.repository.owner.login }}
  REPO_BRANCH: ${{ github.ref_name }}
  ENVIRONMENT_NAME: ${{ github.event.inputs.botEnvironmentType || 'PRODUCTION' }}
  ENVIRONMENT_NAME_LOWER: '' # Will be set in the sanitize job
  PORT: ${{ github.event.inputs.port || '1234' }} # Default port is 1234 but should probably be changed to 8080 (convention)
  NAME_BOT_TOKEN_SECRET: 'BOT_TOKEN_${{ github.event.inputs.botEnvironmentType }}_${{ github.ref_name }}'

jobs:
  sanitize:
    name: Sanitize inputs
    if: ${{ github.event_name == 'workflow_dispatch' || github.ref_name == github.event.repository.default_branch }} # Haven't tried, but I don't think you can specify this in the 'on:' block
    runs-on: ubuntu-latest

    steps:
      - name: Lowercase vars which need to be lowercase
        run: |
          echo "IMAGE_NAME=$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
          echo "CONTAINER_NAME=$(echo "${{ env.CONTAINER_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
          echo "REPO_NAME=$(echo "${{ env.REPO_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Create ENVIRONMENT_NAME_LOWER
        run: |
          echo "ENVIRONMENT_NAME_LOWER=$(echo "${{ github.event.inputs.botEnvironmentType }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

  publish:
    needs: sanitize
    name: Publish image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Build and push Docker image
        run: |
          docker build . --tag ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }} \
            --build-arg GITHUB_USERNAME=${{ secrets.GITHUB_USERNAME }} \
            --build-arg GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }} \
            --build-arg REPO_USERNAME=${{ env.REPO_USERNAME }} \
            --build-arg REPO_NAME=${{ env.REPO_NAME }} \
            --build-arg REPO_BRANCH=${{ env.REPO_BRANCH }}

          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

  deploy:
    needs: publish
    name: Deploy image
    runs-on: ubuntu-latest

    steps:
      - name: Install SSH keys
        run: |
          install -m 600 -D /dev/null ~/.ssh/id_rsa
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SSH_HOST }} > ~/.ssh/known_hosts

      - name: Deploy
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
          cd ${{ secrets.WORK_DIR }}
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          docker stop ${{ env.CONTAINER_NAME }} || true
          docker rm ${{ env.CONTAINER_NAME }} || true
          docker run -d --name ${{ env.CONTAINER_NAME }} -p ${{ env.PORT }}:${{ env.PORT }} \
            -v ${{ secrets.CONFIG_PATH }}:/app/${{ env.ENVIRONMENT_NAME_LOWER }}.config.json \
            -e "JAVA_OPTS=-Dbot.token=${{ secrets[env.NAME_BOT_TOKEN_SECRET] }} \
            -Ddb.mongodb.uri=${{ secrets.MONGO_URI }} \
            -Dbot.environment=${{ env.ENVIRONMENT_NAME }} \
            -Dbot.config=/app/${{ env.ENVIRONMENT_NAME_LOWER }}.config.json \
            -Dhypixel.key=${{ secrets.HYPIXEL_KEY }}" \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          EOF

      - name: Cleanup SSH keys
        run: rm -rf ~/.ssh